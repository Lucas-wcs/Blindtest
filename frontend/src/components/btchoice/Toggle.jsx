/* import React, { useState } from "react";

function Toggle() {
  const [difficulty, setDifficulty] = useState(true);

  const chooseDifficulty = () => {
    setDifficulty(!difficulty);
  };

  return (
    <div
      className={difficulty ? "toggle-containerBlue" : "toggle-containerOrange"}
    >
      <button
        id={difficulty ? "toggleBlue" : "toggleOrange"}
        type="button"
        onClick={chooseDifficulty}
      >
        {difficulty ? "FACILE" : "DIFFICILE"}
      </button>
    </div>
  );
}

export default Toggle; */
